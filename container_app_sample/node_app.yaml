apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: node-app
  labels:
    app: node-app
spec:
  replicas: 2 # Adjust the number of replicas as needed
  selector:
    matchLabels:
      app: node-app 
  template:
    metadata:
      labels:
        app: node-app
    spec:
      containers:
        - name: node-app
          image: baluaddagatla/container_app_sample:1.0.26
          ports:
            - containerPort: 3000 # Adjust the port as needed
          env:
            - name: NODE_ENV
              value: "production" # Set environment variables as needed
      volumes:
        - name: node-app-storage
          persistentVolumeClaim:
            claimName: my-pvc