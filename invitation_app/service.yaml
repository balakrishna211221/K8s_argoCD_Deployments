apiVersion: v1
kind: Service
metadata:
  name: invitcation
  namespace: default
  labels:
    app: invitcation
spec:
  type: NodePort
  selector:
    app: invitcation
  ports:
    - name: invitcation
      targetPort: 3000
  sessionAffinity: None
